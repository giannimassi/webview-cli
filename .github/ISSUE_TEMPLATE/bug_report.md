---
name: Bug report
about: Something doesn't work as expected
labels: bug
---

**macOS version** (`sw_vers -productVersion`):
**Architecture** (`uname -m`):
**webview-cli version** (`webview-cli --help 2>&1 | head -1`):

**What I ran:**
```bash
# full command + stdin
```

**What I expected:**

**What actually happened** (include stderr output):

**Minimal repro JSONL (if A2UI mode):**
```jsonl

```
